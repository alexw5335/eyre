namespace main

dllimport Kernel32 {
	WriteFile
	ExitProcess
}

var string db "This is a test\0"

main:
	sub rsp, 56
	mov ecx, -11
	lea rdx, [string]
	mov r8d, string::size
	mov r9, [rsp + 40]
	mov qword [rsp + 32], 0
	call WriteFile
	call ExitProcess