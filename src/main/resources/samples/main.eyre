namespace main

dllimport Kernel32 {
	WriteFile
	ExitProcess
}

var string db "Message\0"

var message res 8

main:
	sub rsp, 56
	mov byte [message], 0x30
	mov ecx, -11
	lea rdx, [message]
	mov r8d, 1
	mov r9, [rsp + 40]
	mov qword [rsp + 32], 0
	call WriteFile
	call ExitProcess